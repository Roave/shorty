'use strict';

const describe = require('mocha').describe;
const it = require('mocha').it;
// const Protocol = require('../lib/protocol');


describe('SMPP protocol', () => {
    it.skip('placeholder', () => {
    });
});
